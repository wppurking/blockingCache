# Application dependencies

require:
    - play
    - localModule -> blockingCache 0.1

repositories:    
    - localModule:
        type:       local
        artifact:   "${application.path}/../dist/[module]-[revision].zip"
        contains:
            - localModule -> *